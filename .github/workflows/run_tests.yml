name: 'Run tests'

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Update
      run: sudo apt update
    - name: Install apt dependencies
      run: sudo apt install python3 python-wheel-common
    - name: Install requirements
      run: pip install -r requirements.txt
    - name: Run tests
      run: pytest
      timeout-minutes: 5
